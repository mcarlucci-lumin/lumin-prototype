import { Component } from '@angular/core';

/**
 * "Desktop Basic" chrome — a static visual of a client website: shared top
 * nav, the routed prototype in a centered content column, and the shared footer.
 */
@Component({
    selector: 'app-desktop-basic-chrome',
    standalone: false,
    templateUrl: './desktop-basic-chrome.component.html',
    styleUrls: ['./desktop-basic-chrome.component.scss'],
})
export class DesktopBasicChromeComponent {}
